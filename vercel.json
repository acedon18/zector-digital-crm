{
  "git": {
    "deploymentEnabled": {
      "main": true
    },
    "submodules": true
  },
  "buildCommand": "if [ -d \"pulse-main\" ]; then cd pulse-main && npm install && npm run build; else echo \"pulse-main directory not found\"; find . -type d | sort; exit 1; fi",
  "outputDirectory": "pulse-main/dist",
  "installCommand": "echo 'Skipping root install' && git submodule update --init --recursive",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
